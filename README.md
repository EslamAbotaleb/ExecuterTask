# ExecuterTask
Executer task  for many long operations
1. Use semaphore to implment multiple tasks as the same with user when interact with in app.
2. Use tableview to display all tasks name was execute
